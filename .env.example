# Entelgia Production Configuration

# Memory Security
MEMORY_SECRET_KEY=your-secret-key-here-change-in-production-32-chars-min

# ---------------------------------------------------------------------------
# Grok (xAI) Configuration
# Required only when using the Grok LLM backend.
#
# NOTE: LLM backend (Ollama or Grok) and per-agent model names are selected
# interactively at startup when you run Entelgia — NOT via environment
# variables.  The only value you need to set here is GROK_API_KEY so that
# Entelgia can authenticate with the xAI API when Grok is chosen at startup.
#
# How to get a Grok API key:
#   1. Visit https://console.x.ai and sign in with your X (Twitter) account.
#   2. In the left sidebar click "API Keys".
#   3. Click "Create API Key", give it a name, and copy the generated key.
#   4. Paste the key as the value of GROK_API_KEY below.
#
# Available Grok models (selectable at startup):
#   grok-3, grok-3-fast, grok-3-mini, grok-3-mini-fast, grok-2-1212
# ---------------------------------------------------------------------------
GROK_API_KEY=

# Logging
LOG_LEVEL=INFO

# Database
DB_PATH=entelgia_data/entelgia_memory.sqlite

# Sessions
SESSIONS_DIR=entelgia_data/sessions

# API
API_HOST=0.0.0.0
API_PORT=8000
