# Core dependencies
requests>=2.31.0
colorama>=0.4.6
python-dotenv>=1.0.0

# Web research module
beautifulsoup4>=4.12.0

# API server (optional if running REST mode)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Optional utilities
python-dateutil>=2.8.2

# Optional: semantic similarity in Fixy's _detect_repetition
# Install with: pip install "entelgia[semantic]"  or  pip install sentence-transformers scikit-learn
# sentence-transformers>=2.2.0
# scikit-learn>=1.0.0

# Development / testing (optional)
pytest>=7.4.0
pytest-mock>=3.12.0

# Windows-only: replace broken pyreadline with Python 3.10-compatible fork
pyreadline3>=3.4.1; sys_platform == "win32"

