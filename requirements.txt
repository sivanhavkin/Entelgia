# Core dependencies
requests>=2.31.0
colorama>=0.4.6
python-dotenv>=1.0.0

# Web research module
beautifulsoup4>=4.12.0

# API server (optional if running REST mode)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Semantic similarity — core dependency
# Used by EnhancedMemoryIntegration (context_manager.py) and
# CircularityGuard / LoopGuard / Fixy semantic loop detection.
sentence-transformers>=2.2.0
scikit-learn>=1.0.1
numpy>=1.24.0

# Development / testing
pytest>=7.4.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Windows-only: replace broken pyreadline with Python 3.10-compatible fork
pyreadline3>=3.4.1; sys_platform == "win32"

